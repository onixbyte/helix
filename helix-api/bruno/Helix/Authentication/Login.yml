info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseURL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "helix",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
