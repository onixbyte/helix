opencollection: 1.0.0

info:
  name: Helix
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  headers:
    - name: Accept-Language
      value: "{{language}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
