info:
  name: Add Department
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseURL}}/departments"
  body:
    type: json
    data: |-
      {
        "name": "Domestic Marketing",
        "parentId": 10,
        "sort": null,
        "status": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
