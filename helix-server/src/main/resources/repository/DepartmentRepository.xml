<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onixbyte.helix.repository.DepartmentRepository">
    <select id="selectAll" parameterType="com.onixbyte.helix.domain.common.PageRequest"
            resultType="department">
        SELECT id,
        name,
        parent_id,
        sort,
        status,
        created_at,
        updated_at
        FROM departments
        <if test="pageRequest.paged">
            OFFSET #{pageRequest.offset} LIMIT #{pageRequest.pageSize}
        </if>
        <if test="pageRequest.sort != null and pageRequest.sort.sorted">
            ORDER BY
            <foreach collection="pageRequest.sort.orders" item="order">
                ${order.property} ${order.direction}
            </foreach>
        </if>
    </select>

    <select id="selectById" parameterType="long" resultType="department">
        SELECT id,
               name,
               parent_id,
               sort,
               status,
               created_at,
               updated_at
        FROM departments
        WHERE id = #{id}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id" parameterType="department">
        INSERT INTO departments(name, parent_id, sort, status, created_at, updated_at)
        VALUES (#{entity.name}, #{entity.parentId}, #{entity.sort}, #{entity.status},
                #{entity.createdAt}, #{entity.updatedAt})
    </insert>
</mapper>