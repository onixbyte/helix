<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onixbyte.helix.repository.SettingRepository">
    <select id="selectOneById" resultType="com.onixbyte.helix.domain.entity.Setting" parameterType="long">
        SELECT id, name, description, "value", default_value, created_at, updated_at
        FROM settings
        WHERE id = #{id}
    </select>

    <select id="selectOneByName" resultType="com.onixbyte.helix.domain.entity.Setting" parameterType="string">
        SELECT id, name, description, "value", default_value, created_at, updated_at
        FROM settings
        WHERE name = #{name}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id" parameterType="com.onixbyte.helix.domain.entity.Setting">
        INSERT INTO settings(name, description, "value", default_value)
        VALUES (#{setting.name}, #{setting.description}, #{setting.value}, #{setting.defaultValue})
    </insert>
</mapper>