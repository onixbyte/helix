:80 {
	# API reverse proxy — strip the /api prefix and forward to the server container
	handle_path /api/* {
		reverse_proxy server:8080
	}

	# SPA static files with client-side routing fallback
	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
